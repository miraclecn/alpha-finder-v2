schema_version = 1
artifact_type = "fundamental_research_input_build_case"
case_id = "fundamental_rerating_core_duckdb"
description = "Build the slower fundamental_rerating observation inputs from the isolated V2 DuckDB once an audited residual component snapshot exists for the same target surface."
sleeve_path = "config/sleeves/fundamental_rerating_core.toml"
source_db_path = "output/research_source.duckdb"
output_path = "output/fundamental_rerating_core_input.json"
residual_component_snapshot_path = "output/open_t1_to_open_t20_residual_component_snapshot.json"
start_date = "20260305"
min_listing_days = 120
lookback_days = 20
turnover_window_days = 20
rebalance_stride = 5
industry_label_source = "industry_classification_pit"
industry_schema = "sw2021_l1"
limit_lock_mode = "cn_a_directional_open_lock"
